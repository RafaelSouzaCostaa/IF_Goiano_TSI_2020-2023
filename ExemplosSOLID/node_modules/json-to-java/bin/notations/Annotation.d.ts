import { Dictionary } from 'lodash';
import { JsonArray, JsonObject } from '../utils/json';
import JavaBaseWithName from './BaseWithName';
export declare function parseAnnotations(emitter: any, fieldName: string, receiver: JavaAnnotation[], annotationJson: JsonArray, currentIndent: number): void;
export default class JavaAnnotation extends JavaBaseWithName {
    readonly values: Dictionary<string> | string | null;
    constructor(currentIndent: number, json: JsonObject);
    toString(): string;
    private valuesString;
}
