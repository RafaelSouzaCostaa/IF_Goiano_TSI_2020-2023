import { JsonArray, JsonObject } from '../../utils/json';
import JavaBaseWithName from '../BaseWithName';
export declare function parseVariableDifinitions(emitter: any, fieldName: string, receiver: JavaVariableDifinition[], argumentJson: JsonArray, currentIndent: number): void;
export default class JavaVariableDifinition extends JavaBaseWithName {
    readonly final: boolean;
    readonly type: string;
    constructor(currentIndent: number, json: JsonObject);
    toString(): string;
}
