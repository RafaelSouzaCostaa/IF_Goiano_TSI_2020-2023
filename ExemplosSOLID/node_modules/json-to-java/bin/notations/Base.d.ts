export default abstract class JavaBase {
    nameWhenAsEmitter: string;
    readonly currentIndentString: string;
    private readonly currentIndent;
    constructor(currentIndent: number);
    contentIndentString(depth?: number): string;
}
